
---
# 📚 Шпаргалка по Git!

## ✨ Основные команды Git

### Инициализация репозитория
- Создать новый локальный репозиторий:  
  ```bash
  git init
  ```

- Склонировать удалённый репозиторий:  
  ```bash
  git clone <URL_репозитория>
  ```

### Работа с ветками
- Посмотреть активные ветки:  
  ```bash
  git branch
  ```

- Переключиться на существующую ветку:  
  ```bash
  git checkout <название_ветки>
  ```

- Создать новую ветку:  
  ```bash
  git branch <название_новой_ветки>
  ```

- Удалить локальную ветку:  
  ```bash
  git branch -d <название_ветки>
  ```

### Управление изменениями
- Просмотр изменений файлов:  
  ```bash
  git diff
  ```

- Добавление всех изменений к следующему коммиту:  
  ```bash
  git add .
  ```

- Создание нового коммита:  
  ```bash
  git commit -m "<сообщение>"
  ```

- Откат последнего коммита (оставляя изменения):  
  ```bash
  git reset HEAD~1
  ```

### Синхронизация с удалённым репозиторием
- Отправить изменения на сервер:  
  ```bash
  git push origin main
  ```

- Получить обновления с сервера:  
  ```bash
  git pull origin main
  ```

### Конфликты слияния
- Ручное разрешение конфликтов (редактирование конфликта вручную):  
  После редактирования выполните:  
  ```bash
  git add . && git commit
  ```

### Регистрация на GitHub
1. Перейдите на сайт https://github.com/.
2. Зарегистрируйтесь и создайте аккаунт.
3. Настройте SSH-ключи для безопасной связи с серверами GitHub (необязательно).

---
# 1. Хеши в Git
Каждый объект в репозитории Git получает уникальный идентификатор — хэш SHA-1. Именно хеш гарантирует уникальность и неизменность объектов в хранилище.

Что такое хеш SHA-1?Хеш SHA-1 — это криптографический алгоритм, генерирующий фиксированную длину уникальной последовательности символов (обычно 40 шестнадцатеричных цифр). Он обеспечивает целостность данных, позволяя проверять, что файл не был изменён с момента первого сохранения.

Зачем нужен хеш?— Уникальное идентификация коммитов и объектов.— Быстрая проверка целостности данных.— Быстрое сравнение состояний репозитория.



---
# 2. Команда git log
git log — одна из важнейших команд для просмотра истории изменений в проекте. Она показывает хронологию коммитов с датами, авторами и сообщениями.

## Основные опции команды:

git log: простой просмотр журнала коммитов.
git log --oneline: компактный вид журнала с одним коротким комментарием.
git log --graph: визуализация структуры веток и слияний.
git log --stat: показать статистику изменений по каждому коммиту.
Полезные сочетания клавиш:  

j/k: прокрутка вверх-вниз.  
q: выход из журнала.


---

# 3. Термины: HEAD и рабочая область
## HEAD — специальная метка, указывающая на последний применённый коммит. По сути, это вершина текущего состояния репозитория. Важно понимать, что HEAD движется вслед за каждым вашим действием (фиксация, переход на ветку и т.п.).

Рабочая область (working directory) — это папка на вашем компьютере, куда Git сохраняет текущие копии файлов. Все изменения сначала вносятся в рабочую область, а потом фиксируются в историю.


---
# 4. Жизненный цикл файлов и статусы в Git
Статус	Определение
Untracked	Новый файл, не учтённый Git
Modified	Изменённый файл
Staged	Готов к фиксации
Committed	Зафиксировано в истории
Схема перехода статусов:

mermaid
graph TD;
untracked["Untracked"] -- "git add" --> staged["Staged"];
staged -- "git commit" --> committed["Committed"];
modified["Modified"] -- "git add" --> staged;
committed -- "Изменения внесены" --> modified;
** Описание схемы:Файлы проходят этапы от непрослеживаемых (untracked) до готовности к фиксации (staged), а затем окончательно сохраняются в истории (committed). Из стадии "зафиксированных" (committed) файлы возвращаются обратно в состояние "модифицированные" (modified) после внесения изменений. **


---
# 5. Оформление сообщений к коммитам
**Хорошее оформление сообщений к коммитам облегчает дальнейшую навигацию по истории и повышает качество разработки. **
## Несколько рекомендаций:

Краткий и точный заголовок (<50 символов).  
Используйте повелительное наклонение глаголов (например, "Исправил баг", "Обновил документацию").  
Сообщение должно ясно отражать суть изменений.  
Включайте ссылки на тикеты системы отслеживания ошибок (например, #123).
Примеры хороших сообщений:  

"Добавлена поддержка Java 24.0"  
"Оптимизировано кэширование запросов (#456)."  
"Исправлен баг в обработчике формы."



---
# *Изменение коммитов* 
--amend рассчитан на работу с последним коммитом (HEAD).
Дополнить коммит новыми файлами можно с помощью git commit --amend --no-edit. Благодаря опции --no-edit сообщение к коммиту останется таким, каким и было.
Изменить сообщение к коммиту позволяет команда git commit --amend -m "Обновлённое сообщение коммита".


--- 
# Откат коммитов 
*Команда git restore --staged <file> убирает файлы из списка коммита.


*Команда git reset --hard <commit hash> откатывает историю до указанного коммита.


*Команда git restore <file> восстанавливает изменения файла до последней сохраненной версии.





#Инициализация репозитория
##git init (от англ. initialize, «инициализировать») — инициализируй репозиторий.


##Синхронизация локального и удалённого репозиториев

###git remote add origin https://github.com/YandexPracticum/first-project.git (от англ. remote, «удалённый» + add, «добавить») — привяжи локальный репозиторий к удалённому с URL https://github.com/YandexPracticum/first-project.git;
###git remote -v (от англ. verbose, «подробный») — проверь, что репозитории действительно связались;
###git push -u origin main (от англ. push, «толкать») — в первый раз загрузи все коммиты из локального репозитория в удалённый с названием origin.
💡 Ваша ветка может называться master, а не main. Подправьте команду, если это необходимо.
###git push (от англ. push, «толкать») — загрузи коммиты в удалённый репозиторий после того, как он был привязан с помощью флага -u.


#Подготовка файла к коммиту
###git add todo.txt (от англ. add, **«добавить») — подготовь файл todo.txt к коммиту;
###git add --all (от англ. add, «добавить» + all, «всё») — подготовь к коммиту сразу все файлы, в которых были изменения, и все новые файлы;
###git add . — подготовь к коммиту текущую папку и все файлы в ней.


#Создание и публикация коммита
###git commit -m "Комментарий к коммиту." (от англ. commit, «совершать», фиксировать» + message, «сообщение») — сделай коммит и оставь комментарий, чтобы было проще понять, какие изменения сделаны;
###git push (от англ. push, «толкать») — добавь изменения в удалённый репозиторий.


#Просмотр информации о коммитах
###git log (от англ. log, «журнал [записей]») — выведи подробную историю коммитов;
###git log --oneline (от англ. log, «журнал [записей]» + oneline, «одной строкой») — покажи краткую информацию о коммитах: сокращённый хеш и сообщение.


#Просмотр состояния файлов
###git status (от англ. status, «статус», «состояние») — покажи текущее состояние репозитория.


#Добавление изменений в последний коммит
###git commit --amend --no-edit (от англ. amend, «исправить») — добавь изменения к последнему коммиту и оставь сообщение прежним;
###git commit --amend -m "Новое сообщение" — измени сообщение к последнему коммиту на Новое сообщение.
💡 Выйти из редактора Vim: нажать Esc, ввести :qa!, нажать Enter.


#«Откат» файлов и коммитов
###git restore --staged hello.txt (от англ. restore, «восстановить») — переведи файл hello.txt из состояния staged обратно в untracked или modified;
###git restore hello.txt — верни файл hello.txt к последней версии, которая была сохранена через git commit или git add;
###git reset --hard b576d89 (от англ. reset, «сброс», «обнуление» + hard, «суровый») — удали все незакоммиченные изменения из staging и «рабочей зоны» вплоть до указанного коммита.


#Просмотр изменений
###git diff (от англ. difference, «отличие», «разница») — покажи изменения в «рабочей зоне», то есть в modified-файлах;
###git diff a9928ab 11bada1 — выведи разницу между двумя коммитами;
###git diff --staged — покажи изменения, которые добавлены в staged-файлах.